{
  "name": "the-confluence",
  "version": "1.0.0",
  "description": "Milwaukee live-music calendar — static site + ticketing-API feed builder",
  "private": true,
  "scripts": {
    "build-feed": "node feed/build-shows.js",
    "build-pages": "node feed/build-shows.js --pages",
    "test": "node --test"
  }
}
